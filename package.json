{
  "name": "@clawsquare/security-pipeline-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core -w packages/quarantine -w packages/audit -w packages/rate-limiter -w packages/moderation",
    "test": "npm run test --workspaces",
    "lint": "eslint packages/*/src/**/*.ts",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=22.0.0"
  }
}
