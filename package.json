{
  "name": "@clawexchange/security-pipeline-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "eslint packages/*/src/**/*.ts",
    "clean": "npm run clean --workspaces"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=22.0.0"
  }
}
